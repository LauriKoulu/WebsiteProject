extends layout

block content
  div#app
    div.container.flow-text
      div.row
        div.col.s6
          h2.center= title
          section(v-if="message.length")
            p= message
      form(action="/signup", method="post")
        div.row
          div.col.s6
            label(for="email") E-mail
             input(name="email", type="text", class="validate", placeholder="Your email", id="input-field")
          div.col.s6
        div.row
          div.col.s6
            label(for="password") Password
            input(name="password", type="password", class="validate", placeholder="Your password", id="input-field")
          div.col.s6
        div.row
          div.col.s6
            button(class="btn waves-effect waves-light", type="submit", name="action") Signup
          div.col.s6


